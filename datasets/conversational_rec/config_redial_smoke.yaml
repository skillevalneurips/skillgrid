_base_: config_redial.yaml

experiment:
  name: conversational_rec_redial_smoke
  max_episodes: 1
  skill_creation:
    probe_tasks: 3

dataset:
  max_samples: 3
  max_train_samples: 3
  max_test_samples: 3
