_base_: ../../configs/base.yaml

experiment:
  name: aime_axes
  protocol: react
  max_episodes: 10
  max_steps_per_episode: 12
  num_evolution_rounds: 1
  max_skills: 4
  react:
    visibility: labels
    enable_verify: true
    lb_sample_size: 2     # LB: show 2 random skills per task; 0/neg = show all
    lb_seed: 42           # LB: per-run seed (mixed with task_id); change to resample
  skill_creation:
    probe_tasks: 5

dataset:
  name: aime
  train_hf: math-ai/aime24
  test_hf: math-ai/aime25
  max_train_samples: 10
  max_test_samples: 15
  split_seed: 42
